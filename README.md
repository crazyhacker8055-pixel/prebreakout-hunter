# Pre-Breakout Hunter V30
Strict pre-breakout scanner + independent Smart Live Event Radar + fast refresh continuity.

V30 fixes live-refresh continuity by storing and reusing the exact full-scan structure pool. It also keeps the previous full-scan event snapshot visible when a refresh changes the current live classification. Strict thresholds remain unchanged.
